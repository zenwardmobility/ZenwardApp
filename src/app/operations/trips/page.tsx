import { OperationsRouteStub } from "@/components/scaffold/OperationsRouteStub";

export default function TripsPage() {
  return (
    <OperationsRouteStub
      title="Trips"
      description="Structural placeholder — the canonical Trips list is built in a later phase."
      breadcrumb={[{ label: "Trips" }]}
    />
  );
}
