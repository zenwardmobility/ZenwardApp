import { OperationsRouteStub } from "@/components/scaffold/OperationsRouteStub";

export default function FleetPage() {
  return (
    <OperationsRouteStub
      title="Fleet"
      description="Structural placeholder — vehicle/fleet management is built in a later phase."
      breadcrumb={[{ label: "Fleet" }]}
    />
  );
}
