import { OperationsRouteStub } from "@/components/scaffold/OperationsRouteStub";

export default function DispatchPage() {
  return (
    <OperationsRouteStub
      title="Dispatch"
      description="Structural placeholder — the canonical Dispatch Board is built in a later phase."
      breadcrumb={[{ label: "Dispatch" }]}
    />
  );
}
