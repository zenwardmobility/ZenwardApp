import { OperationsRouteStub } from "@/components/scaffold/OperationsRouteStub";

export default function DriversPage() {
  return (
    <OperationsRouteStub
      title="Drivers"
      description="Structural placeholder — driver management is built in a later phase."
      breadcrumb={[{ label: "Drivers" }]}
    />
  );
}
