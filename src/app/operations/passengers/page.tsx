import { OperationsRouteStub } from "@/components/scaffold/OperationsRouteStub";

export default function PassengersPage() {
  return (
    <OperationsRouteStub
      title="Passengers"
      description="Structural placeholder — passenger records are built in a later phase."
      breadcrumb={[{ label: "Passengers" }]}
    />
  );
}
