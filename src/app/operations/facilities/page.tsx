import { OperationsRouteStub } from "@/components/scaffold/OperationsRouteStub";

export default function FacilitiesPage() {
  return (
    <OperationsRouteStub
      title="Facilities"
      description="Structural placeholder — facility records are built in a later phase."
      breadcrumb={[{ label: "Facilities" }]}
    />
  );
}
